{
  "name": "sms-inbox-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:remote": "wrangler d1 migrations apply sms-inbox --remote"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
